{
  "name": "chapter-forge",
  "version": "0.1.0",
  "description": "banking SDLC harness — agents by RACI role, commands for the 8 phases, reference skills, guardrail hooks (block PII/secret + dangerous commands), a pipeline graph, and the chapter-context MCP that taps into context across the whole polyrepo.",
  "author": {
    "name": "Chapter Engineering"
  },
  "keywords": ["sdlc", "banking", "compliance", "pci-dss", "guardrail", "devsecops"],
  "hooks": "./hooks/hooks.json",
  "mcpServers": {
    "chapter-context": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/chapter-context/dist/index.js"],
      "env": {
        "CHAPTER_WORKSPACE": "${CHAPTER_WORKSPACE}"
      }
    }
  }
}
